# =============================================================================
# הריפו הזה ציבורי — jsDelivr מגיש רק מריפו ציבורי.
# לכן הכלל כאן הפוך מהרגיל: **הכל חסום**, ורק המנוע נפתח במפורש.
#
# הסיבה: hydropop.site.js ממילא גלוי לכל גולש (הוא רץ בדפדפן שלו), ולכן
# פרסומו לא מוסיף חשיפה. כל השאר — אסטרטגיה, מספרי המרה, הפאנל, ההגדרות
# והפרומפטים — אינם חשופים היום, ואין סיבה שייחשפו.
#
# להוסיף קובץ לפרסום? הוסף שורת !שם-הקובץ. בזהירות.
# =============================================================================

# הכל חסום כברירת מחדל
*

# ...ורק אלה עוברים
!.gitignore
!hydropop.site.js
